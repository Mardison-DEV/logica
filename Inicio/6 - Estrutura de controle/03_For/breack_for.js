for (let a = "@"; a <= "@@@@@@@@@@@@"; a += "@"){
        if (a === "@@@@")
        break;
        console.log(a)
}

for (let b = 1; b <=10; b++){
        if (b === 3)
        continue;
        if (b === 6)
        continue;
        if (b === 9)
                continue;

    console.log(b);
}

